[package]
name = "example_with_photon"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nano = { path = "../../" }
photon = { path = "../../crates/photon" }